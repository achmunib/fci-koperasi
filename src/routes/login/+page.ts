// Enable SSR for faster initial load
export const ssr = true;
// Prerender if possible for even faster loads
export const prerender = false;
// Enable client-side navigation
export const csr = true;
